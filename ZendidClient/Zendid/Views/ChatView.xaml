<Page x:Class="Zendid.Views.ChatView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Zendid.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChatView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <!-- Friends Tab View -->
        <Border Margin="5" CornerRadius="15" BorderThickness="0" Background="{StaticResource BackgroundDarkBrush}">
            <StackPanel>
                <Border Margin="5" CornerRadius="10" BorderThickness="0" Background="{StaticResource BackgroundVeryDarkBrush}">
                    <StackPanel>
                        <TextBlock Text="Welcome" Style="{StaticResource Main2}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Page>
